[package]
name = "risearch"
version = "0.1.0"
edition = "2024"

[features]
default = []
# FM-Index backend (work in progress)
fm-index = ["genedex"]
prof = []

[dependencies]
anyhow = "1.0"
clap = { version = "4.5.53", features = ["derive"] }
env_logger = { version = "0.11", features = ["color"] }
genedex = { version = "0.2.2", default-features = false, features = [
    "savefile",
], optional = true }
itoa = "1.0"
libsais = { version = "0.2", default-features = false }
log = { version = "0.4", features = ["release_max_level_info"] }
zmij = "0.1"
needletail = "0.6.3"
serde = { version = "1.0.228", features = ["derive"] }
bincode = "1.3.3"
rayon = "1.11"
savefile = "0.20.2"
smallvec = "1.15.1"

[dev-dependencies]
assert_cmd = "2.1.1"
criterion = { version = "0.5", features = ["html_reports"] }
flate2 = "1.1.0"
predicates = "3.1.3"
tabled = "0.18"
tempfile = "3.24.0"
rstest = "0.24"

# [[bench]]
# name = "seed_search"
# harness = false

# [[bench]]
# name = "c_bench"
# harness = false

[profile.release]
debug = true
lto = false        # Enable LTO for cross-crate inlining
codegen-units = 1   # Consistent symbol resolution
